// Note - useState
/*

*/
const Counter = () => {
  const handleCounter = () => {};

  return (
    <div>
      <h1>{}</h1>
      <button onClick={handleCounter}>+</button>
    </div>
  );
};

export default Counter;
